# 草稿箱（Private Drafts）

这个文件夹存放**未发布**的草稿文章。

## 说明

- 草稿文件存放在这里，**不会**出现在博客首页
- 当草稿完成后，通过 `manage.html` 发布，文件会移动到 `posts/` 文件夹
- Git推送时，这个文件夹的内容也会上传到GitHub，但不会在博客上显示

## 如果想要真正的私密（不上传到GitHub）

在项目根目录创建 `.gitignore` 文件，添加：

```
drafts/
```

这样草稿文件就只存在于本地，不会被推送到GitHub。